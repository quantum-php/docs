version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_build:
      - mkdir -p .rtd-docs
      - cp README.md .rtd-docs/index.md
      - cp -r v3.0 .rtd-docs/

mkdocs:
  configuration: mkdocs.yml
